export class Role {
    roleid: number;
    title: string;
    permissions: any;
    constructor(obj: Role) {
        this.roleid = obj.permissions;
        this.title = obj.permissions;
        this.permissions = obj.permissions;
    }
}
